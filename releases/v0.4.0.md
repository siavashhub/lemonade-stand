# v0.4.0

## Added
- **Bundled tools** bundled `uv` binary and Electron Node resolver so MCP servers work without user installs
- **UX updates** updated UI fora better UX

## Fixed
- **Planner not working** added logging and startup config
